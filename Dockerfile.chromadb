# Use a suitable Python base image
FROM python:3.11-slim

# Set the working directory inside the container
WORKDIR /usr/src/app

# Install any necessary dependencies for ChromaDB
# Assuming chromadb is a Python package, install it via pip
RUN pip install chromadb

# Expose the port where ChromaDB will run (adjust according to ChromaDB's default port)
EXPOSE 8000

# Command to start ChromaDB as a server
CMD ["chromadb", "start", "--host", "0.0.0.0", "--port", "8000"]
